import '../styles/ChatHeader.css';

function ChatHeader() {
    return (
        <header className="Header">
            AI Shift Assistant
        </header>
    );
}

export default ChatHeader;

//This is the header for the AI Chat window.
